<nav class="navbar navbar-expand-lg navbar-dark bg-dark text-white mb-4">
  <div class="container">
    <%= link_to 'Rendezvous', root_path, class: 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if authorized? %>
          <li class="nav-item"><%= link_to 'Calendars', calendars_path, class: 'nav-link' %></li>
        <% end %>
        <li class="nav-item"><%= link_to 'Locations', locations_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Users', users_path, class: 'nav-link' %></li>

        <%# Only display the current user's name, friend requests and log out links if a user is logged in %>
        <% if logged_in? %>
          <li class="nav-item"><%= link_to current_user.full_name, current_user, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'Friend Requests', '/friend_requests', class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'Log out', '/logout', method: :delete, class: 'nav-link' %></li>
        <% else %>
        <%# Only display the sign up and log in links if no user is logged in %>
          <li class="nav-item"><%= link_to 'Sign up', new_user_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'Log in', '/login', class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
